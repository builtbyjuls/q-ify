# Copy this file to .env. Keep real values local and out of Git.
# Hosted JDBC URLs must use direct PostgreSQL endpoints with TLS required.
# Keep credentials in the dedicated fields, not embedded in either URL.
SPRING_DATASOURCE_URL=
SPRING_DATASOURCE_USERNAME=
SPRING_DATASOURCE_PASSWORD=
SPRING_FLYWAY_URL=
